meta {
  name: List place action links
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/v1/accounts/:id/gbp/place-actions
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  The Book, Order and Reserve buttons shown on the profile.

  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
