meta {
  name: Get the service list
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/v1/accounts/:id/gbp/services
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
