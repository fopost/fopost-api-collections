meta {
  name: Delete a knowledge source
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/v1/knowledge/sources/:id
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Removes the source and every passage indexed from it.

  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
