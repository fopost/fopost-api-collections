meta {
  name: Clear Telegram bot commands
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/v1/accounts/:id/telegram/commands
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
