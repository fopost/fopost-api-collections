meta {
  name: List or search server members
  type: http
  seq: 44
}

get {
  url: {{baseUrl}}/v1/accounts/:id/discord/members
  body: none
  auth: inherit
}

params:query {
  ~q: 
  ~limit: 100
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
