meta {
  name: Get health summary for all accounts
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/accounts/health
  body: none
  auth: inherit
}

params:query {
  ~workspaceId: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
