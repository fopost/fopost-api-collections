meta {
  name: List YouTube playlists
  type: http
  seq: 38
}

get {
  url: {{baseUrl}}/v1/accounts/:id/youtube/playlists
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  The connected channel's own playlists, with the stored default marked.

  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
