meta {
  name: Clear ice breakers
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/v1/accounts/:id/messaging/ice-breakers
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
