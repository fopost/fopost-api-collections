meta {
  name: Delete a message
  type: http
  seq: 64
}

delete {
  url: {{baseUrl}}/v1/accounts/:id/discord/messages/:messageId
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
  messageId: {{messageId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `publish` scope when using an API key.

  **Scope:** `publish`
}
