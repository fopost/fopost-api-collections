meta {
  name: Clear the greeting
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/v1/accounts/:id/messaging/greeting
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
