meta {
  name: Get the webhook subscription
  type: http
  seq: 19
}

get {
  url: {{baseUrl}}/v1/accounts/:id/webhook-subscription
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  What the network currently delivers to our webhook for this account. `subscribed` is false when the subscription lapsed or a required field is missing.

  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
