meta {
  name: Unpin a message
  type: http
  seq: 36
}

delete {
  url: {{baseUrl}}/v1/accounts/:id/discord/messages/:messageId/pin
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
  messageId: {{messageId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `publish` scope when using an API key.

  **Scope:** `publish`
}
