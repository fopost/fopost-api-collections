meta {
  name: Delete a custom field
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/v1/contacts/fields/:id
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Removes the field and every answer to it. This cannot be undone.

  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
