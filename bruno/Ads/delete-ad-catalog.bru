meta {
  name: Delete a product catalog
  type: http
  seq: 19
}

delete {
  url: {{baseUrl}}/v1/ads/catalogs/:id?workspace_id=&connection_id=
  body: none
  auth: inherit
}

params:query {
  workspace_id: 
  connection_id: 
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Deletes it on Meta with every product, feed and set beneath it.

  Requires the `ads` and `publish` scopes when using an API key.

  **Scope:** `ads`
}
