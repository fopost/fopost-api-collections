meta {
  name: Read a campaign ad schedule
  type: http
  seq: 23
}

get {
  url: {{baseUrl}}/v1/ads/google/ad-schedule?connection_id=&customer_id=&campaign_id=
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
  connection_id: 
  customer_id: 
  campaign_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
