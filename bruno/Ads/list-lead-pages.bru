meta {
  name: List Pages feeding the leads feed
  type: http
  seq: 81
}

get {
  url: {{baseUrl}}/v1/ads/lead-pages
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
