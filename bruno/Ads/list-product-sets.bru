meta {
  name: List product sets
  type: http
  seq: 57
}

get {
  url: {{baseUrl}}/v1/ads/catalogs/:id/product-sets?connection_id=
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
  connection_id: 
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  A product set is what a catalog ad promotes.

  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
