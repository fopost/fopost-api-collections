meta {
  name: Create a custom audience
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/v1/ads/audiences
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "workspaceId": "{{workspaceId}}",
    "connectionId": "00000000-0000-0000-0000-000000000000",
    "adAccountId": "x",
    "name": "Example name",
    "spec": {
      "subtype": "CUSTOM"
    }
  }
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([201]);
  });
}

docs {
  A customer list (`CUSTOM`, emails are hashed before they leave), a lookalike of an existing audience (`LOOKALIKE`), website visitors from a pixel (`WEBSITE`), a list of companies (`COMPANY_LIST`) or people who engaged with a page, ad, form or video (`ENGAGEMENT`). Which subtypes a network takes follows its capabilities.

  Requires the `ads` scope when using an API key.

  **Scope:** `ads`

  **Optional fields** (not in the example body):
  - `description`: string
}
