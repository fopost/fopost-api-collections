meta {
  name: Upload conversion adjustments
  type: http
  seq: 41
}

post {
  url: {{baseUrl}}/v1/ads/google/conversions/adjustments
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "workspaceId": "{{workspaceId}}",
    "connectionId": "00000000-0000-0000-0000-000000000000",
    "customerId": "string"
  }
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([201]);
  });
}

docs {
  Restate, retract or enhance a conversion already counted.

  Requires the `publish` scope when using an API key.

  **Scope:** `publish`
}
