meta {
  name: Delete an ad label
  type: http
  seq: 44
}

delete {
  url: {{baseUrl}}/v1/ads/account/labels/:id?workspace_id=&connection_id=&ad_account_id=
  body: none
  auth: inherit
}

params:query {
  workspace_id: 
  connection_id: 
  ad_account_id: 
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
