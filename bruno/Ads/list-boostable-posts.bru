meta {
  name: List posts that can be boosted
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/v1/ads/boostable
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Published posts with a delivery on an account a Meta Ads connection reaches.

  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
