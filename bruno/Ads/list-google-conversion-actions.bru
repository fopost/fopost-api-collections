meta {
  name: List conversion actions
  type: http
  seq: 38
}

get {
  url: {{baseUrl}}/v1/ads/google/conversions?connection_id=&customer_id=
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
  connection_id: 
  customer_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
