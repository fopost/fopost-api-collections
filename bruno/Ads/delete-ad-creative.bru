meta {
  name: Delete a creative
  type: http
  seq: 43
}

delete {
  url: {{baseUrl}}/v1/ads/creatives/:id?workspace_id=&connection_id=
  body: none
  auth: inherit
}

params:query {
  workspace_id: 
  connection_id: 
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
