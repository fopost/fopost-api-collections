meta {
  name: List ad networks
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/v1/ads/providers
  body: none
  auth: inherit
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Every ad network, with whether it can be connected on this deployment.

  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
