meta {
  name: Get insights over a date range
  type: http
  seq: 94
}

get {
  url: {{baseUrl}}/v1/ads/insights?connection_id=&object_id=&since=&until=
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
  connection_id: 
  object_id: 
  since: 
  until: 
  ~breakdown: age
  ~daily: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Totals for an ad account (`act_…`), campaign, ad set or ad, optionally split by one breakdown and by day.

  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
