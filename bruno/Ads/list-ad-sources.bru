meta {
  name: List ad accounts and Pages
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/v1/ads/sources
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Every ad connection with the ad accounts and, where the network has them, the Pages its grant reaches.

  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
