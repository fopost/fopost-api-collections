meta {
  name: Read what a conversion rule recorded
  type: http
  seq: 59
}

get {
  url: {{baseUrl}}/v1/ads/linkedin/conversion-rules/:id/metrics?connection_id=&since=&until=
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
  connection_id: 
  since: 
  until: 
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
