meta {
  name: List Meta Ads connections
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/v1/ads/connections
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
