meta {
  name: List Instant Forms
  type: http
  seq: 47
}

get {
  url: {{baseUrl}}/v1/ads/lead-forms
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Every Page a Meta Ads connection reaches, with its lead forms.

  Requires the `ads` scope when using an API key.

  **Scope:** `ads`
}
