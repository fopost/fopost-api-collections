meta {
  name: Get automation stats
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/automations/stats
  body: none
  auth: inherit
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Totals plus run counts by status for the last 24 hours and the 10 latest runs.

  Requires the `automations` scope when using an API key.

  **Scope:** `automations`
}
