meta {
  name: Hide a comment on the platform
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/v1/inbox/:id/hide
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
