meta {
  name: Unhide a comment on the platform
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/v1/inbox/:id/unhide
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
