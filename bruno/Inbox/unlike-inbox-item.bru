meta {
  name: Remove our like
  type: http
  seq: 19
}

post {
  url: {{baseUrl}}/v1/inbox/:id/unlike
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Only where `canLike` is true. Also requires the `publish` scope.

  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
