meta {
  name: Count unread items
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/inbox/unread-count
  body: none
  auth: inherit
}

params:query {
  ~workspace_id: 
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
