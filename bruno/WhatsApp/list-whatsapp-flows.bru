meta {
  name: List WhatsApp flows
  type: http
  seq: 26
}

get {
  url: {{baseUrl}}/v1/accounts/:id/whatsapp/flows
  body: none
  auth: inherit
}

params:path {
  id: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  The in-chat forms on this account, with their status and validation errors.

  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
