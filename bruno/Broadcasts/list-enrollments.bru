meta {
  name: List a sequence’s enrollments
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/v1/sequences/:id/enrollments
  body: none
  auth: inherit
}

params:query {
  ~page: 1
  ~per_page: 50
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Who is on it, what step they are at, and when the next one is due.

  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
