meta {
  name: Get a broadcast
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/broadcasts/:id
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `inbox` scope when using an API key.

  **Scope:** `inbox`
}
