meta {
  name: Search X communities
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/accounts/:accountId/communities/search?q=
  body: none
  auth: inherit
}

params:query {
  q: 
}

params:path {
  accountId: {{accountId}}
}

tests {
  test('status is successful', function () {
    expect(res.getStatus()).to.be.oneOf([200]);
  });
}

docs {
  Requires the `accounts` scope when using an API key.

  **Scope:** `accounts`
}
